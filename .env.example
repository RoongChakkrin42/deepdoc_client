# Base URL of deepdoc_server, called from the browser.
#
# Note that .env.local overrides this file in Next.js. If requests go somewhere
# unexpected, check .env.local first.
NEXT_PUBLIC_BACKENDURL=http://localhost:8000
